// Stub for the `server-only` package under Vitest (plain Node, not Next's
// bundler), which is what actually enforces the "server component only"
// import restriction. See vitest.config.ts alias.
export {};
